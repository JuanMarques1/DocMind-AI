# Blueprint do Render: backend (Docker) + banco Postgres.
# Importe em https://dashboard.render.com → New → Blueprint, apontando para este repo.
databases:
  - name: docmind-db
    plan: free
    databaseName: docmind
    user: docmind

services:
  - type: web
    name: docmind-api
    runtime: docker
    plan: free
    # Caminhos relativos à RAIZ do repositório (padrão de monorepo no Render).
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /api/health
    envVars:
      # Connection string do Postgres acima (injetada automaticamente).
      - key: DATABASE_URL
        fromDatabase:
          name: docmind-db
          property: connectionString
      
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4o-mini
      
      - key: FRONTEND_ORIGIN
        sync: false
    
      - key: SECRET_KEY
        generateValue: true
      
      - key: HSTS_ENABLED
        value: "true"
